# 08 — Agent添加参考家具及表单联动

**What to build:** Agent使用目录家具生成参考方案，用户采用、调整或移除。

**Blocked by:** 04 — 推荐式追问与一键采用建议; 06 — 点击家具弹出属性编辑器

**Status:** done
**Phase:** Week 2 / Phase 1
**建议负责人:** 后端+前端
**粗估:** 1–1.5人日（待接入探针校准）

## Acceptance criteria
- [x] 默认家具由白名单asset_id实例化
- [x] 参考家具关联suggestion_id，场景与右栏同时显示未采用状态
- [x] 采用整套方案时展示参数范围并原子更新
- [x] 移除参考家具不删除用户已确认的其他需求
- [x] 数量变化后的对象ID可追踪且重试不重复创建

## 验证边界
验证端到端可见行为与对应错误分支；数据一致性、权限和并发必须覆盖。实际命令由开工时的仓库发现，不把建议命令冒充已有验证。
不实现后续票；没有AI生图任务。遵循用户草图、选定OpenPlan3D、Pi和共享确认语义。

## Comments / execution receipt

### Final coordinator acceptance — 2026-09-13
- Restored done after two actual P1 repros: cross-room/floor movement of accepted refs and non-unit scaling inconsistent with confirmed dimensions. Both old-code tests failed, then fixed with final transaction/reference/object guards; explicit dimensions required. Unsupported scale/adoption and accepted cross-room transfer return409 without version/event/requirement mutation. Same-room edits still synchronize confirmed attributes.
- Real-provider reference08 browser1/1 passed: whitelist plan, pending native objects/panel, quantity stable IDs, visible exact scope, dismissal/atomic adoption/removal and reload. Final31/31 API/logic pass. Evidence: week2-final-20260913T143835Z-8483a6/evidence/mobile-fix-final/browser-results.json plus coordinator-review.json; prior red/green paths in docs/WEEK2-VALIDATION.md.
- Final source fingerprint3d3c20ae3c633641d362a44a0359e27fb60b64aaebe1341c62710192c3e450d4. Dense mobile labels/markers can still overlap; no label-avoidance claim. Production not deployed; limits/rollback in CURRENT.

- 2026-09-11：由用户新版草图重生成；尚无实现或验收证据。
- 开工时填写：HEAD固定点、受保护未提交文件、验收现状分类、实际命令。
- 完成时填写：变更、逐项证据、剩余风险与回滚方式。

- Started week2-ticket08-20260913T114143Z-b52849 at HEAD5d77da1; all pre-existing source/delegation/evidence protected. Scope08 plus new-project test isolation. Sequential isolated builds/tests only.

### Implementation receipt — week2-ticket08-20260913T114143Z-b52849
- HEAD5d77da1; all prior source/delegation/evidence retained.08 remains in-progress: final scene/side-panel simultaneous visual acceptance needs recheck after screenshot-discovered layout repair. No acceptance wording changed.04/06 accepted dependencies read;07 retained,09+ not implemented.
- Implemented apps/api/references.ts real Pi bounded whitelist tool (chair/storage/bookshelf), strict quantities and unique asset lines, same-turn identical tool results deduplicated,60-second abort. Raw user input/plan status persist, request replay never repeats model; provider/version failure preserves evidence. Store migration defaults older reference_plans. Native scene references carry stable object_id/room_id/suggestion_id/reference_status; native ThreeViewer uses actual procedural catalog objects and yellow 未采用 reference sprites, never labels existing confirmed sample furniture.
- Versioned transactional group/single accept/remove/quantity endpoints bind owner/room/plan/object; all selected objects validate before commit; append separate confirmed furniture requirements, never overwrite existing fields. Room-position validation rejects references moved outside bound room. Removals refuse accepted objects and preserve all requirements. Quantity decrease/reincrease reuses reserved IDs. Manual properties use existing06 editor. Generic scene/save now rejects forged reference metadata, bypassed acceptance and direct reference deletion.
- Frontend ReferencePanel in right panel: raw request, pending/formal status, quantity adjustment, single/group scope dialog with exact IDs/assets/dimensions/color/elevation, dismiss without mutation, explicit accept/remove. Dirty scene/preview blocks reference commands. Final UI labels target quantity and actual scene count separately; adjusting target may restore removed reference IDs. No geometry-warning implementation or placement safety claim.
- Isolation support: main.tsx optional ?project=owned-id boot fetch, no fallback on invalid/unauthorized ID; default behavior retained. helpers.login now creates real POST /api/projects and navigates persisted query URL. Existing callers checked: login once at scenario start; reload tests stay on same ID. No fixture deletion.
- Tests: services.log/services-final.log/services-room-final.log1/1 each, latest includes whitelist, quantities, ownership, wrong room, invalid assets, ID reuse, replay, missing confirmation, group invalid member all-or-nothing, stale CAS, protected existing requirements/objects, forged scene status, moved-object room rejection. npm run check passed (check-final and check-room-final). Full isolated web+engine build passed in build.log; subsequent web build passed in build-web-final.log. Final layout build logged build-layout.log.
- Actual configured Pi via authenticated HTTP/browser: browser08-final.log1/1 passed. Fresh owned v0 project; model creates2 chairs+1 storage,3 native reference label IDs, right-panel pending3, scope IDs displayed/dismissed unchanged requirements, remove1 then group accept2, reload same URL/objects/requirements, native pending labels0; no unexpected console/page errors. Invalid project boot handled without iframe fallback. Initial browser08.log failed only post-reload bridge readiness, retained; poll now waits for committed native load. This is not a mock or generator-only probe.
- Screenshots directly inspected: .runtime/codex-runs/week2-ticket08-20260913T114143Z-b52849/evidence/final/08-scope-desktop.png and08-mobile.png. Scope is readable and exact, but long reference panel stretched page and hid native scene above viewport. Repaired with max-height46dvh and internal scrolling after this run; final layout not yet browser reverified. Native label existence was asserted, but screenshot of visible native pending labels alongside right panel still REQUIRED before marking remaining criterion/done. Next: focused08 browser on latest isolated web+engine, capture top-level pending scene desktop/mobile before opening scope; check quantity adjustment UI and final layout. No need to rerun broad Week1 while developing; coordinator comprehensive regression later.
- Production4173/live.data/production bundles/tunnel untouched;4175 only synthetic test data, exited before rebuilds. No reset/clean/commit/dependency upgrade/sandbox change. Rollback scoped08 and isolation hunks only; preserve all previous changes and private evidence.

- week2-ticket09-20260913T115159Z-b6a864 predecessor recheck: fresh web build and focused browser1/1 passed; desktop screenshot actually shows3 native pending labels beside right-panel pending objects, window.scrollY=0 even long scope list.375px screenshot shows full-height panel still occluding scene. Added mobile bottom-sheet height46dvh/reference scroll24dvh in current candidate; not reverified at375px yet.08 remains in-progress for that explicit mobile requirement. Evidence:.runtime/codex-runs/week2-ticket09-20260913T115159Z-b6a864/evidence/08.

### Mobile gate closed — week2-ticket09mic-20260913T121836Z-cbd2a1
- Read prior completion evidence directly, not its timeout outcome: .runtime/codex-runs/week2-ticket09finish-20260913T120407Z-d09d47/evidence/browser.log passes08 plus real09 media; microphone failure was separate.08 pending desktop and375px captures under browser/08-pending-*.png show actual native 未采用 sprites simultaneously with the right/bottom panel, topbar visible and document scrollY0 assertions passed.46dvh bottom sheet/24dvh internal reference scroll resolves the previous occlusion. Existing catalog/adoption/backend evidence remains intact; no08 backend reimplementation.08 now done. Production unchanged.

- 2026-09-13 week2-hardening-20260913T124402Z-499f9d：独立复核回归修复。手动表单绑定加载时服务器CAS版本、仅发改动字段、409保留草稿；AI采用关闭旧表单；参考已确认属性与对象/普通场景保存同步，历史证据保留。focused8/8，07/08/10真实浏览器3/3；新增数据丢失回归真实Pi通过。证据见本run evidence及ticket10回执；尚未部署。
