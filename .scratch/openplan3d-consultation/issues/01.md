# 01 — OpenPlan3D接入与可恢复工作台

**What to build:** 在顶部菜单下浏览一个OpenPlan3D样例房间，旋转、缩放、进入房间，并保存恢复。

**Blocked by:** None — can start immediately

**Status:** done
**Phase:** Week 1 / Phase 0
**建议负责人:** 前端主导，后端配合
**粗估:** 2–3人日（待接入探针校准）

## Acceptance criteria
- [x] 锁定上游commit、许可证与样例素材来源
- [x] React壳内自托管OpenPlan3D视图可加载且不会出现第二套冲突菜单
- [x] 旋转、缩放、房间聚焦可用
- [x] 一个对象的选择ID可回传外层
- [x] 修改一个支持属性后JSON保存再载入保持对象ID和属性
- [x] 基础项目访问控制拒绝未授权读取

## 验证边界
验证端到端可见行为与对应错误分支；数据一致性、权限和并发必须覆盖。实际命令由开工时的仓库发现，不把建议命令冒充已有验证。
不实现后续票；没有AI生图任务。遵循用户草图、选定OpenPlan3D、Pi和共享确认语义。

## Comments / execution receipt
- 2026-09-11：由用户新版草图重生成；尚无实现或验收证据。
- 开工时填写：HEAD固定点、受保护未提交文件、验收现状分类、实际命令。
- 完成时填写：变更、逐项证据、剩余风险与回滚方式。

### Implementation session — 2026-09-11
- Scope: week1, tickets 01–05, sequential dependency gates. Ticket01 started.
- Baseline HEAD: 388e26a02294643681a947f964d3c319e8b5c478. Existing untracked planning, handoffs, taskboard and tunnel files protected; no reset/clean.
- Runtime: sandbox has Python 3.12.3, no node/npm in PATH. Existing 127.0.0.1:4173 health identifies renovation-taskboard; keep it and tunnel intact.
- Validation plan: pinned upstream source; production build; actual browser bridge round-trip; backend authorization/version/idempotency tests. Commands will be recorded after creation.

### Week 1 implementation receipt — 2026-09-11
- Implementation: OpenPlan3D pin + native iframe bridge + React workbench + PostgreSQL scene/auth implementation.
- Evidence: `docs/handoffs/WEEK-1.md`, `docs/ENGINE.md`, `docs/evidence/week1/unit-tests.log`, `provider-probe.json`, `walkthrough.json`, `http-smoke.json`. Test fixtures are synthetic; secrets and customer data are not published.
- Gate: ticket 01 browser/WebGL and responsive acceptance remains blocked by the execution environment. Related UI criteria and dependent tickets are NOT accepted merely because code exists.
- Deployment: existing 4173 taskboard and tunnel unchanged; safe PID verification and cutover remain open. Preview startup: `bash scripts/run-workbench.sh` (4174).
- Rollback: no public service was replaced. Preserve original untracked files and owner `.data`; do not reset/clean.

### Local-server acceptance and deployment — 2026-09-11
- Scope: resume Week 1 tickets 01–05, browser gates and authorized production deployment. HEAD 388e26a02294643681a947f964d3c319e8b5c478; all existing untracked implementation/intake/plans protected.
- Plan: install normal system Chromium dependencies, build and run isolated 4175 browser checks; verify each dependency and acceptance criterion before completion; install supervised 4173 service preserving /todo and existing tunnel.
- Verified old listener: PID 552582, ubuntu, /usr/bin/python3 taskboard/server.py, cwd project root, socket 127.0.0.1:4173. Recheck immediately before cutover.

- Browser gate passed on local system Chromium after normal system dependency installation: real recursive raycast selected sofa-main, native JSON restored ID/color, orbit/zoom/focus/walk/2D, 1440/768/375 screenshots and no page errors. Fixed embedded furniture raycasting, steeper room camera and hidden duplicate engine controls. Evidence: docs/evidence/week1/01-engine-results.json and 01-*.png. Production cutover tracked in CURRENT.
