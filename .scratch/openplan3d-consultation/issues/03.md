# 03 — 悬浮聊天与Pi流式咨询

**What to build:** 左下角按钮打开assistant-ui聊天弹窗，发送文本给Pi并保存恢复。

**Blocked by:** 01 — OpenPlan3D接入与可恢复工作台

**Status:** done
**Phase:** Week 1 / Phase 0
**建议负责人:** 后端主导，前端配合
**粗估:** 1.5–2人日（待接入探针校准）

## Acceptance criteria
- [x] 弹窗支持打开关闭，关闭后消息和输入草稿保留
- [x] 真实provider完成流式文本与一次工具调用探针
- [x] 响应与当前项目/房间绑定
- [x] 取消、超时或模型错误后用户原消息仍保留
- [x] 断线重连按事件ID去重
- [x] 输入框聚焦时删除/旋转等场景快捷键不触发

## 验证边界
验证端到端可见行为与对应错误分支；数据一致性、权限和并发必须覆盖。实际命令由开工时的仓库发现，不把建议命令冒充已有验证。
不实现后续票；没有AI生图任务。遵循用户草图、选定OpenPlan3D、Pi和共享确认语义。

## Comments / execution receipt
- 2026-09-11：由用户新版草图重生成；尚无实现或验收证据。
- 开工时填写：HEAD固定点、受保护未提交文件、验收现状分类、实际命令。
- 完成时填写：变更、逐项证据、剩余风险与回滚方式。

### Week 1 implementation receipt — 2026-09-11
- Implementation: Real Pi streaming/provider/tool probe passed. Chat evidence, cancellation/error retention, room-bound tools and SSE replay implemented; browser keyboard/draft/reconnect acceptance pending.
- Evidence: `docs/handoffs/WEEK-1.md`, `docs/ENGINE.md`, `docs/evidence/week1/unit-tests.log`, `provider-probe.json`, `walkthrough.json`, `http-smoke.json`. Test fixtures are synthetic; secrets and customer data are not published.
- Gate: ticket 01 browser/WebGL and responsive acceptance remains blocked by the execution environment. Related UI criteria and dependent tickets are NOT accepted merely because code exists.
- Deployment: existing 4173 taskboard and tunnel unchanged; safe PID verification and cutover remain open. Preview startup: `bash scripts/run-workbench.sh` (4174).
- Rollback: no public service was replaced. Preserve original untracked files and owner `.data`; do not reset/clean.

### Local-server acceptance — 2026-09-11
- Dependency 01 browser gate passed; scope is remaining browser acceptance, preserving all earlier implementation/intake changes at HEAD 388e26a02294643681a947f964d3c319e8b5c478. Tests use only 4175 and .runtime/browser-test-data.

- Accepted: browser workflow verifies draft reopen, keyboard isolation and 375px chat; live-browser.json verifies real model suggestions, cancellation input retained after reload, offline/online replay without duplicate messages. Fixed offline/online EventSource lifecycle using last event ID. Backend tests cover provider failure, cancellation during initialization, ordered replay. Earlier real provider probe remains valid.
