# 13 — 设计师看板与角色协作

**What to build:** 业主与设计师分别登录同一项目，看板可追溯需求并定位房间家具。

**Blocked by:** 05 — 初步需求任务书与审查; 06 — 点击家具弹出属性编辑器

**Status:** done
**Phase:** Week 3 / Phase 2
**建议负责人:** 后端主导
**粗估:** 1–1.5人日（待接入探针校准）

## Acceptance criteria
- [x] 服务端按项目成员和角色验证访问，越权测试被拒绝
- [x] 设计师看板显示需求、预算、来源和未决项
- [x] 点击需求可回看原话并定位关联对象
- [x] 设计师的建议不被标记成业主确认
- [x] 保存权限与只读入口界面一致

## 验证边界
验证端到端可见行为与对应错误分支；数据一致性、权限和并发必须覆盖。实际命令由开工时的仓库发现，不把建议命令冒充已有验证。
不实现后续票；没有AI生图任务。遵循用户草图、选定OpenPlan3D、Pi和共享确认语义。

## Comments / execution receipt
- 2026-09-11：由用户新版草图重生成；尚无实现或验收证据。
- 开工时填写：HEAD固定点、受保护未提交文件、验收现状分类、实际命令。
- 完成时填写：变更、逐项证据、剩余风险与回滚方式。

### Ticket 13 implementation and acceptance receipt — 2026-09-16
- Baseline HEAD fixed at `b2aba327c7b80dd882216e28e6be90f371c48c2a`. Existing dirty worktree changes were protected; this ticket added only the collaboration files and the scoped edits listed below.
- Added project-scoped designer invite records and role/project/member-aware sessions. A designer session is bound to one project; non-whitelisted project GETs and all owner writes are rejected with 403. Regenerating an invite revokes the previous invite.
- Added `GET /api/projects/:id/designer-board`, `GET /api/projects/:id/collaboration/view`, owner `POST /api/projects/:id/collaboration/invite`, and designer-only `POST /api/projects/:id/collaboration/suggestions`. Shared projections filter unauthorized intake answers, evidence, attachments, chat, reports, snapshots and suggestions.
- Added the designer board UI with requirements, budget, source/original wording, pending items, room/furniture location, owner invite display, and a separate designer suggestion form. Designer workbench controls are read-only and owner-only save/adopt/questionnaire controls are hidden or disabled.
- Changed files: `apps/api/app.ts`, `apps/api/store.ts`, `apps/api/collaboration.ts`, `packages/contracts/index.ts`, `apps/web/src/main.tsx`, `apps/web/src/Workbench.tsx`, `apps/web/src/RequirementsPanel.tsx`, `apps/web/src/DeliveryPanel.tsx`, `apps/web/src/DesignerBoard.tsx`, `apps/web/src/collaboration.css`, `tests/collaboration.test.ts`, `tests/browser/13-collaboration.spec.ts`.
- Verification actually run: `npm run check` passed; `npm run build:web` passed; `./node_modules/.bin/tsx --test tests/collaboration.test.ts` passed 1/1, including cross-project/detail/scene-save 403s and same-version concurrent suggestion 200/409 with one persisted write; `./node_modules/.bin/tsx --test tests/foundation.test.ts tests/intake-v2.test.ts` passed 9/9; `PLAYWRIGHT_BROWSERS_PATH=$PWD/.runtime/browsers ... npx playwright test tests/browser/13-collaboration.spec.ts` passed 1/1. Browser evidence is in the Playwright test-results directory and includes the mobile screenshot.
- Full `npm test` was started against the existing suite but did not produce a final summary within the execution window; its spawned test processes were verified as this run's repository processes and terminated. It is not counted as a full-suite pass.
- Remaining risks: only the generated project invite grants designer access; there is no email/SSO or expiry UI. Shared scene geometry is intentionally available for locating furniture, while private intake/chat/review data is filtered. Production `4173`, tunnel and owner data were not restarted or changed.
- Rollback: apply the saved scoped patches in reverse (`/tmp/roomnote-ticket13*.patch` on the remote host) or revert only the listed ticket files/hunks; preserve all unrelated dirty files.

### Production publication receipt — 2026-09-16 UTC

- Published frozen runtime: `/opt/renovation-workbench/releases/ticket13-20260916T160000Z/app`; service cut over at 16:08:36–16:08:42 UTC without changing the tunnel or credentials.
- Before the switch, the single writer was stopped and the live `.data` directory was archived to `/var/backups/renovation-workbench/ticket13-20260916T160000Z/owner-data.tar` (SHA-256 `4947db1e332b7e7b1e4cf768dc70dc56dffb9d9d59db9eea96463833b20a08b5`).
- Post-switch checks passed: active service working directory, local and public `/healthz`, and the public `index-BjMYqaR6.js` bundle containing `设计师看板`. Previous hashed web assets remain in the new release for open pages.
- Code rollback: stop `renovation-workbench.service`, restore `/opt/renovation-workbench/control/ticket13-20260916T160000Z/previous-release.conf` to `/etc/systemd/system/renovation-workbench.service.d/20-release.conf`, daemon-reload, then start the service. Keep current data; do not restore an old archive over it.
