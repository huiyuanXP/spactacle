# Paper UI — published and verified, 2026-09-16

`https://prod.huiyuanxp.com/new-ui` is live, with `/new-ui/` and project-query deep links. Same application on127.0.0.1:4173, original `/`, login, owner data and `/todo` preserved. No extra public port or tunnel change. Release `paper-ui-20260916T150735Z`; application PID1521186 / systemd MainPID1521166 at verification. Coding-tools-mcp remains PID831281, without reset/restart.

## Scope and artifact identity

This publishes the accepted03/04/09 Paper UI extension and narrow07 shared-composer work. Those release gates are done. Workspace has newer collaboration implementation/tests and changes to app/store/contracts; they were deliberately excluded and left untouched. No Ticket13, full-house or Week3 completion is claimed.

The frozen runtime comes from `new-ui-20260916T132021Z-f802da/candidate-final-source-web.tar.gz`, SHA256 `6e3764db5a10f83d11c2016b884b8f2c82437eb0fc558aaa5e5b7c154b897bac`, exactly77 manifested runtime files, source fingerprint `f7995e18431b6698fdef64e999ebfcb2fe9852ce1014980d9daa6e5ceb542ca5`.51,541 frozen files include Node24.21.0, both dependency trees and Radix2.1.24;645 engine files match intake-v2. Runtime is `/opt/renovation-workbench/releases/paper-ui-20260916T150735Z/app`; full code fallback is `/opt/renovation-workbench/releases/intake-v2-20260915T083433Z/app`. No runtime links to mutable workspace code. Existing private.env and authoritative.scratch links retained.

## Host verification

- Typecheck passed;51/51 logic/API checks passed in groups27+24. Fresh rebuild with native Vite config loading exactly matches accepted JS `index-C48VyUHk.js`, CSS `index-TJMEpMvL.css` and index HTML.
- Frozen-service browser checks: desktop/phone sheets2/2; desktop workspace1pass; phone focused1/1; live-provider/model/cancel, request recovery and actual ASR3/3. All7 distinct new UI scenarios have fresh passing receipts, **not a single clean whole-suite exit**. Existing14-scenario prior acceptance remains as documented below; it was not rerun wholesale here.
-8 synthetic projects passed new→old→new initialization with answers, optional model_id metadata, attachments, all existing rows and sequences preserved. Separate frozen startup/API/resource/board smoke passed.
- Controlled cutover: writer stopped15:30:52 UTC; candidate healthy15:31:19;54 local/public verification groups completed15:31:41. Entire.data archive was extracted and all1001 file hashes matched both restored copy and unchanged stopped source. Owner-copy new/old/new initialization preserved rows/sequences.
- Pre-stop authenticated session and all existing owner fields retained; baseline1project,2attachments,79commands,128events,9sessions. Actual attachment and original-variant bytes verified locally/publicly. Exact new frontend and native assets, models catalogue, questionnaire/delivery, auth/private-file checks and16-ticket board passed.
- Final **real public browser** passed authenticated `/new-ui` and `/new-ui/` query deep links and reload, canvas/model controls, dark theme, mobile viewport, and original `/`. Existing owner project matched before/after;0 mutating API calls and0 page errors. No owner test projects or model-generation calls.

## Retained failures and environment limits

Initial host unit command endedSIGTERM143 without completion; split checks completed later. First unit-b had23passes/1fixture setup failure because the new evidence directory was missing; runner setup corrected, full24/24 rerun passed. Initial Vite build could not write temporary config into root-owned frozen dependencies; native config loading rebuilt identical artifacts without making runtime writable.4175 was reoccupied by another workflow after the verified historical orphan PID1486071 was stopped; startup refused correctly, and tests used independently verified-free4176 with isolated candidate data. Desktop+phone attempt had a phone navigation ERR_ABORTED; unchanged focused phone passed. Standalone Playwright API client received HTML; verification through actual browser pages and same-origin fetch passed. Logs/traces are retained; no failure is relabeled successful.

The original handoff's hardware/model limits still apply: no physical microphone/real Safari certification, and not every catalogue model was exercised. No sandbox, credentials or MCP service change.

Evidence: `.runtime/deploy-paper-ui-20260916T150735Z/` (baseline, manifest binding, verification.json, source-only candidate, logs, frozen-smoke.json and public-browser.json); fresh browser JSON/screenshots/traces under its `candidate/app/.runtime/codex-runs/paper-ui-20260916T150735Z-*`; sanitized `.runtime/deploy-receipts/paper-ui-20260916T150735Z/result.json`. Root-private backup and code rollback are documented in `docs/DEPLOYMENT.md`. Newer workspace changes remain uncommitted and protected; the WIP Git HEAD does not identify the deployed archive by itself.

---

## Historical pre-publication implementation receipt — release status superseded above

# Paper UI implementation and acceptance — 2026-09-16

## Target and status

Run `new-ui-20260916T132021Z-f802da`. User explicitly authorized AWS MCP work and prioritized their uploaded design.md (SHA256 b40395cb095ac8a74afea842e0d0fe5a20dd731fee22276038c7d5f8a1e927e9). Scope: week1/step3+4 /03+04, week2/step4 /09, narrow week2/step2 /07 object-composer reuse. No new geometry, whole-home editing, WASD or Week3 implementation.

**Implementation and isolated acceptance complete; production publication blocked.** Tickets remain in-progress until complete frozen release, single-writer cutover and production checks pass. This does not revoke the accepted existing intake-v2 release.

## Delivered behavior

`/new-ui` and `/new-ui/` use the same real project/query/authentication, native engine, versioned changes and workbench. Paper/sand light-dark themes, closed type roles and16px composer replace decorative chat chrome. Desktop room/scene/chat layout and actual iPhone13 device emulation are covered; the existing `/` workbench shares the new chat components.

Assistant-ui ExternalStoreRuntime/Thread/Message/Composer/Copy remain the chat implementation. Accessible Radix menus provide real file actions and current configured-provider model IDs. Optional model_id is validated before mutations, included in idempotency, primary/formatting turns, both message records and explicit library transcript confirmation. Model picker is not a capability ranking or a certification of every listed model. ASR and image analysis keep their specialized configured services.

Textarea above; + below left; model, microphone and send/stop below right. Real file upload/previews/retry/remove-only-reference and library behavior remain connected. IME/229 is not sent; Shift+Enter and touch Enter insert newlines. Voice becomes editable text and never auto-sends. Interrupted requests preserve both submitted text and newer typing plus attachment references. Switching rooms retains mounted drafts.

Questions now live in the scrollable thread. Actual recommendations, alternatives, free text, optional unknown/skip/not-applicable and provenance remain explicit. No preselected recommendation; confirmation retains conflict and idempotency guards. Real answer receipts link back to the full questionnaire. No unimplemented multi-select/image-choice capability is claimed. Furniture-only chat remains scoped and still requires preview/adoption.

## Verification receipts

- `check-06.log`: final TypeScript check passed (exit0 through the successful browser-07 command chain).
- `unit-02.log`:51/51 unit/API tests passed,0 failed; includes3 new model-binding/auth/idempotency tests. Supplied public/synthetic fixture generator used.
- `build-04.log`: final web build passed; `new-ui-20260916T132021Z-f802da/web-final`; JS index-C48VyUHk.js, CSS index-TJMEpMvL.css. Existing chunk-size warning retained (JS764.67kB, gzip237.10kB); no code-splitting optimization claim.
- `browser-05.log`: all14 existing browser scenarios and6 new scenarios passed (20 receipts), then the MCP600s command deadline sentSIGTERM. The last ASR scenario has **no completion receipt in this attempt**. This is not a full21/21 successful process exit.
- `browser-07.log`: final7 new UI/sheet scenarios passed, exit0, on4176 with a fresh isolated database. Includes real streaming/cancel/model binding, real MediaRecorder/WAV/server ASR, controlled HTTP503 draft preservation, keyboard menus/IME, actual touch device emulation, question confirmation and both themes of questionnaire/delivery.
- Taken together, all21 distinct browser scenarios have passing evidence. Final artifact differs from broad-run build only in CSS focus, shadow, placeholder and native form color-scheme corrections; all7 new scenarios were rerun on the final artifact. Test helper additionally accepts an explicit isolated test directory, with a realpath guard excluding owner data.

Screenshots and Playwright JSON: `.runtime/codex-runs/new-ui-20260916T132021Z-f802da/browser-07/`; final screenshot hashes in `.runtime/new-ui-20260916T132021Z-f802da/results.json`. Visually reviewed final desktop light, phone dark, desktop questionnaire dark and phone delivery light, plus the earlier desktop dark/question-card/menu states. No clipped composer controls or horizontal overflow observed. The final question focus outline is computed-color checked against the ink token, not just class-name inspected.

Retained failed attempts: browser-01 path guard; browser-02 missing private-HOME browser cache; browser-03 two passes plus ambiguous test summary selector (fixed); browser-04 five passes; browser-06 refused occupied4175 before any tests. unit-01 had49 passes and2 missing-fixture setup failures, corrected by preparing the required fixtures. No failures were relabeled passing.

## Source and candidate identity

Initial HEAD `5d77da130167e9e7e40a022ad8dd36e1a6a6361a`. Observed HEAD `b2aba327c7b80dd882216e28e6be90f371c48c2a` was advanced by another workflow to a WIP checkpoint during this run; this run did not commit, reset or clean. All172 original protected source/doc files and dirty status are saved under `.runtime/new-ui-20260916T132021Z-f802da/baseline/` and baseline.json.

Runtime source fingerprint: `f7995e18431b6698fdef64e999ebfcb2fe9852ce1014980d9daa6e5ceb542ca5`.

Final source/web archive: `.runtime/new-ui-20260916T132021Z-f802da/candidate-final-source-web.tar.gz` (451547bytes), SHA256 `6e3764db5a10f83d11c2016b884b8f2c82437eb0fc558aaa5e5b7c154b897bac`. Manifest `.runtime/new-ui-20260916T132021Z-f802da/candidate-final-manifest.json` binds77 runtime source files, final assets and test hashes. This is a source/web handoff, **not a complete production runtime freeze**: dependencies/native runtime/credentials/owner data are not bundled. Earlier candidate-source-web.tar.gz is superseded and retained.

Changed protected files (scoped deltas, not a request to restore wholesale):

- `.scratch/openplan3d-consultation/issues/03.md`
- `.scratch/openplan3d-consultation/issues/04.md`
- `.scratch/openplan3d-consultation/issues/07.md`
- `.scratch/openplan3d-consultation/issues/09.md`
- `apps/api/business-routes.ts`
- `apps/api/chat.ts`
- `apps/api/media.ts`
- `apps/api/provider.ts`
- `apps/web/src/ChatWindow.tsx`
- `apps/web/src/ComposerMedia.tsx`
- `apps/web/src/IntakeCard.tsx`
- `apps/web/src/ObjectChat.tsx`
- `apps/web/src/Workbench.tsx`
- `apps/web/src/main.tsx`
- `docs/handoffs/CURRENT.md`
- `package-lock.json`
- `package.json`
- `packages/contracts/index.ts`
- `tests/browser/09-media.spec.ts`
- `tests/browser/09-microphone.spec.ts`
- `tests/browser/helpers.ts`
- `tests/browser/intake-v2.spec.ts`

New runtime source: `apps/web/src/ChatControls.tsx`, `apps/web/src/components/shared/type.ts`, `apps/web/src/paper.css`. New tests: tests/chat-models.test.ts, tests/browser/new-ui.spec.ts, tests/browser/new-ui-sheets.spec.ts. New design/host handoff documents are under docs/design and docs/handoffs.

## Publication and cleanup boundary

At 2026-09-16T14:15:44.252284+00:00, production health was still application PID1298869; public `/new-ui` still served old index-Cy1-y_d1.js / index-B60uLlYk.css. No owner test projects, no owner model calls, no production source/data/service/tunnel/credential changes. systemctl actually failed with host systemd inaccessible. Current production remains the accepted intake-v2 freeze, as described in docs/DEPLOYMENT.md.

The600s broad-run timeout left a4175 test listener. It reports namespace-local PID100 and is not visible in the next exec's PID namespace; kill_command for the original command returned killed:false. Do not kill host PID100 or bypass namespaces. Host must identify the exact isolated test listener/cwd before cleanup. Final checks used a separately verified-free4176 and new `.runtime/new-ui-20260916T132021Z-f802da/browser-final-data`; normal Playwright cleanup completed and4176 was confirmed closed. No second writer touched the4175 test data or production.data.

Next: use `docs/handoffs/PAPER-UI-HOST-PUBLISH.md` from an authorized host execution entry to clean only the identified orphaned test process, prepare a full frozen release from the final manifest and pinned runtime, verify an isolated copy, take a stopped-writer private backup, cut over and verify the real public new assets/routes/existing data. Preserve intake-v2 as the full code fallback. Update tickets to done only after that receipt exists.

Rollback before publication: production already remains intact; revert only this run's scoped hunks after comparison with protected baseline and the external WIP checkpoint. Do not reset the working tree or overwrite subsequent work. After publication, stop and verify the writer before full code rollback, keep current owner data, never blindly restore an older database archive.

Boundaries: no real Safari/physical microphone hardware test; test microphone input is a public audio fixture while recorder/conversion/upload/ASR are real. Not every catalogue model was live-tested. Production cutover, backup/restore checks and public interaction acceptance remain unrun. No Codex delegation occurred; codex-runs is the pre-existing evidence path guard.
